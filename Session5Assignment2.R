##########################################################################################################################
# Session4Assignment1.R                   
# Shashikant Parashar
# Session:5, Assignment:2
##########################################################################################################################
vec1 = c(rownames(mtcars[1:15,]))
vec2 = c(rownames(mtcars[10:32,]))
##########################################################################################################################
#Obtain the elements of the union between two character vectors.
cbind(union(vec1,vec2))
##########################################################################################################################
#Get those elements that are common to both vectors.
cbind(intersect(vec1,vec2))
##########################################################################################################################
#Get the difference of the elements between two character vectors.
cbind(min(vec1,vec2))
##########################################################################################################################
vec2 = c(rownames(mtcars[11:25,]))
##Test the quality of two character vectors.
vec1 == vec2
## End
